load("//tools:defaults.bzl", "ng_module", "sass_binary")

package(default_visibility = ["//visibility:public"])

ng_module(
    name = "mat-automatic-report",
    srcs = glob(["**/*.ts"]),
    assets = [
        "automatic-report-demo.html",
        ":automatic_report_demo_scss",
    ],
    deps = [
        "//src/material/forms",
        "//src/material/reports",
        "@npm//@angular/router",
        "@npm//rxjs",
    ],
)

sass_binary(
    name = "automatic_report_demo_scss",
    src = "automatic-report-demo.scss",
)
