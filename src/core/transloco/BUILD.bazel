load(
    "//tools:defaults.bzl",
    "markdown_to_html",
    "ng_module",
)

package(default_visibility = ["//visibility:public"])

ng_module(
    name = "transloco",
    srcs = glob(
        ["**/*.ts"],
        exclude = ["**/*.spec.ts"],
    ),
    deps = [
        "@npm//@angular/core",
        "@npm//@ngneat/transloco",
    ],
)

markdown_to_html(
    name = "overview",
    srcs = ["transloco.md"],
)

filegroup(
    name = "source-files",
    srcs = glob(["**/*.ts"]),
)
