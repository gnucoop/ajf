load("//tools:defaults.bzl", "ts_library")

package(default_visibility = ["//visibility:public"])

ts_library(
    name = "private",
    testonly = True,
    srcs = glob(
        ["**/*.ts"],
        exclude = ["**/*.spec.ts"],
    ),
    deps = [
        "@npm//@angular/cdk",
        "@npm//@angular/core",
        "@npm//@types/jasmine",
    ],
)
